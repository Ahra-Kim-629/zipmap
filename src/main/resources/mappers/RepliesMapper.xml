<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.daedong.zipmap.mapper.RepliesMapper">

    <!-- 댓글 목록 조회 -->
    <select id="getRepliesByTarget" resultType="com.daedong.zipmap.domain.Reply">
        SELECT
        r.id,
        r.target_type AS target_type,
        r.target_id AS target_id,
        r.user_id AS userId,
        u.login_id AS loginId,  r.content,
        r.created_at AS createdAt
        FROM reply r
        LEFT JOIN user u ON r.user_id = u.id WHERE r.target_type = #{targetType} AND r.target_id = #{targetId}
        ORDER BY r.created_at DESC
    </select>

    <!-- 댓글 작성 -->
    <insert id="insertReply" parameterType="com.daedong.zipmap.domain.Reply">
            INSERT INTO reply
            (target_type, target_id, user_id, content)
            VALUES
            (#{targetType}, #{targetId}, #{userId}, #{content})
    </insert>

    <!-- 댓글 수정 -->
    <update id="updateReply" parameterType="com.daedong.zipmap.domain.Reply">
        UPDATE reply
        SET content = #{content},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <!-- 댓글 삭제 -->
    <delete id="deleteReply" parameterType="long">
            DELETE FROM reply
            WHERE id = #{id}
    </delete>

    <!-- 댓글 아이디로 찾기 -->
    <select id="findReplyById" resultType="com.daedong.zipmap.domain.Reply">
            SELECT
            r.id,
            r.target_type AS target_type,
            r.target_id AS target_id,
            r.user_id AS userId,
            u.login_id AS loginId,  r.content,
            r.created_at AS createdAt
            FROM reply r
            LEFT JOIN user u ON r.user_id = u.id WHERE r.id = #{id}
    </select>

</mapper>