<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.daedong.zipmap.mapper.SubscriptionMapper">

    <select id="checkDuplicate" resultType="int">
        SELECT COUNT(*)
        FROM keyword_subscription
        WHERE user_id = #{userId}
        AND keyword = #{keyword}
        AND target_type = #{targetType}
    </select>

    <insert id="saveSubscription">
        INSERT INTO keyword_subscription (user_id, keyword, target_type, created_at)
        VALUES (#{userId}, #{keyword}, #{targetType}, NOW())
    </insert>

    <select id="findKeywordsByUserId" resultType="string">
        SELECT keyword
        FROM keyword_subscription
        WHERE user_id = #{userId}
        AND target_type = #{targetType}
    </select>

    <delete id="deleteSubscription">
        DELETE FROM keyword_subscription
        WHERE user_id = #{userId}
        AND keyword = #{keyword}
        AND target_type = #{targetType}
    </delete>

</mapper>