<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.daedong.zipmap.mapper.PostMapper">
    <select id="findById" resultType="com.daedong.zipmap.domain.PostDTO">
        SELECT
        p.id, p.title, p.content, p.user_id, p.category, p.location, p.like_count, p.view_count, p.post_status,
        p.created_at, p.updated_at, u1.login_id as login_id
        FROM post p
        LEFT JOIN user u1 ON p.user_id = u1.id
        WHERE p.id = #{id}
    </select>

    <select id="findAll" resultType="com.daedong.zipmap.domain.PostDTO">
        SELECT
        p.*,
        u.login_id as loginId
        FROM POST p
        LEFT JOIN user u ON p.user_id = u.id
        <where>
            AND p.post_status = 'ACTIVE' <!-- 추가 : 포스트 스테이터스가 ACTIVE인 것만 조회 -->
            <if test="category != null and category != ''">
                AND p.category = #{category.name}
            </if>
            <if test="location != null and location != ''">
                AND p.location = #{location.name}
            </if>
            <if test="keyword != null and keyword != ''">
                <choose>
                    <when test="searchType == 'title' or searchType == null">
                        AND p.title LIKE CONCAT('%', #{keyword}, '%')
                    </when>
                    <when test="searchType == 'content'">
                        AND p.content LIKE CONCAT('%', #{keyword}, '%')
                    </when>
                    <otherwise>
                        AND p.title LIKE CONCAT('%', #{keyword}, '%')
                    </otherwise>
                </choose>
            </if>
        </where>
        ORDER BY
        (CASE WHEN p.category = 'NOTICE' THEN 0 ELSE 1 END) ASC,
        p.id DESC
        LIMIT #{pageable.pageSize} OFFSET #{pageable.offset}
    </select>

    <select id="countAll" resultType="int">
        SELECT COUNT(*) FROM POST
        <where>
            <if test="category != null and category != ''">
                AND category = #{category}
            </if>
            <if test="location != null and location != ''">
                AND location = #{location}
            </if>
            <if test="keyword != null and keyword != ''">
                AND title LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
    </select>

    <select id="findAllByIdList" resultType="com.daedong.zipmap.domain.PostDTO">
        SELECT
        id, title, content, user_id,
        IF(category IS NULL OR category = '', 'FREE', category) as category,
        IF(location IS NULL OR location = '', 'ALL', location) as location,
        like_count, view_count, post_status, created_at, updated_at
        FROM post
        WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        ORDER BY FIELD (id,
        <foreach collection="list" item="id" separator=",">
            #{id}
        </foreach>
        )
    </select>

    <select id="count" resultType="int">
        SELECT count(*)
        FROM Post
        <where>
            <if test="searchType == 'title' and key != null and key != ''">
                AND title LIKE CONCAT('%', #{key}, '%')
            </if>
            <if test="searchType == 'content' and key != null and key != ''">
                AND content LIKE CONCAT('%', #{key}, '%')
            </if>
        </where>
    </select>

    <insert id="insertPost" parameterType="com.daedong.zipmap.domain.Post"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO post (
        title, content, user_id, category, location,
        post_status, created_at, updated_at
        )
        VALUES (
        #{title}, #{content}, #{userId}, #{category}, #{location},
        #{postStatus}, NOW(), NOW()
        )
    </insert>

    <select id="findReplyById" resultType="com.daedong.zipmap.domain.PostDTO">
        SELECT
        r.id,
        r.post_id as postId,
        r.user_id as userId,
        u.login_id as loginId,
        r.content,
        r.created_at as createdAt,
        r.updated_at as updatedAt
        FROM reply r
        JOIN user u ON r.user_id = u.id
        WHERE r.id = #{id}
    </select>

    <update id="updateReply">
        UPDATE reply
        SET content = #{content},
        updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="updatePost" parameterType="com.daedong.zipmap.domain.Post">
        UPDATE post
        SET title = #{title},
        content = #{content},
        updated_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deletePost">
        DELETE FROM post WHERE id = #{id}
    </delete>

    <select id="adminFindAll" resultType="com.daedong.zipmap.domain.PostDTO">
        SELECT
        p.*,
        u.login_id as loginId FROM post p
        LEFT JOIN user u ON p.user_id = u.id
        <where>
            <if test="category != null and category != ''">AND p.category = #{category}</if>
            <if test="location != null and location != ''">AND p.location = #{location}</if>
            <if test="keyword != null and keyword != ''">
                <choose>
                    <when test="searchType == 'title'">AND p.title LIKE CONCAT('%', #{keyword}, '%')</when>
                    <when test="searchType == 'content'">AND p.content LIKE CONCAT('%', #{keyword}, '%')</when>
                    <when test="searchType == 'loginid'">AND u.login_id LIKE CONCAT('%', #{keyword}, '%')</when>
                </choose>
            </if>
        </where>
<!--        ORDER BY p.id DESC-->
        ORDER BY
        (CASE WHEN p.category = 'NOTICE' THEN 0 ELSE 1 END)ASC, p.id DESC
        LIMIT #{pageable.pageSize} OFFSET #{pageable.offset}
    </select>

    <select id="countPosts" resultType="long">
        SELECT COUNT(*) FROM post
        <where>
            <if test="category != null and category != ''">AND category = #{category}</if>
            <if test="location != null and location != ''">AND location = #{location}</if>
            <if test="keyword != null and keyword != ''">
            </if>
        </where>
    </select>

    <update id="updatePostStatus">
        UPDATE post
        SET post_status = #{status},
        updated_at = NOW()
        WHERE id = #{id}
    </update>

    <insert id="insertReaction">
        INSERT INTO post_reaction (post_id, user_id, type, created_at)
        VALUES (#{postId}, #{userId}, #{type}, NOW())
        ON DUPLICATE KEY UPDATE
        type = #{type},
        updated_at = NOW()
    </insert>

    <delete id="deleteReaction">
        DELETE FROM post_reaction
        WHERE post_id = #{postId} AND user_id = #{userId}
    </delete>

    <update id="updateBoardLikeCount">
        UPDATE post p
        SET
        p.like_count = (SELECT COUNT(*) FROM post_reaction WHERE post_id = #{postId} AND type = 1),
        p.dislike_count = (SELECT COUNT(*) FROM post_reaction WHERE post_id = #{postId} AND type = 2)
        WHERE p.id = #{postId}
    </update>

    <!--    <select id="findReaction" resultType="Integer">-->
    <!--        SELECT type-->
    <!--        FROM post_reaction-->
    <!--        WHERE post_id = #{postId} AND user_id = #{userId}-->
    <!--    </select>-->


<!--    내가 쓴 글 조회-->
    <select id="findByUserId" resultType="com.daedong.zipmap.domain.PostDTO">
        SELECT
        id, title, content, category, location, like_count, view_count, post_status, created_at, updated_at
        FROM post
        WHERE user_id = #{userId}
        AND post_status = 'ACTIVE'
        ORDER BY id DESC
        LIMIT #{pageable.pageSize} OFFSET #{pageable.offset}
    </select>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM post
        WHERE user_id = #{userId}
        AND post_status = 'ACTIVE'
    </select>

    <!-- 내가 쓴 댓글 조회 -->
    <!--    <select id="findRepliesByUserId" resultType="com.daedong.zipmap.domain.PostReply">-->
    <!--        SELECT r.id, r.post_id, r.user_id, r.content, r.created_at, r.updated_at, p.title as postTitle-->
    <!--        FROM post_reply r-->
    <!--        JOIN post p ON r.post_id = p.id-->
    <!--        WHERE r.user_id = #{userId}-->
    <!--        ORDER BY r.id DESC-->
    <!--        LIMIT #{pageable.pageSize} OFFSET #{pageable.offset}-->
    <!--    </select>-->

    <!--    <select id="countRepliesByUserId" resultType="int">-->
    <!--        SELECT COUNT(*) FROM post_reply-->
    <!--        WHERE user_id = #{userId}-->
    <!--    </select>-->

    <select id="findMainpagePost" resultType="com.daedong.zipmap.domain.PostDTO">
        <![CDATA[
    SELECT DISTINCT
        id, title, content, category, created_at, likeCount, reasonType, score
    FROM (
        (
            SELECT
                id, title, content, category, created_at,
                like_count AS likeCount,
                like_count AS score,
                'CATEGORY_TOP' AS reasonType
            FROM (
                SELECT id, title, content, category, created_at, like_count,
                       ROW_NUMBER() OVER(PARTITION BY category ORDER BY like_count DESC, created_at DESC) as rn
                FROM post
                WHERE created_at >= DATE_SUB(NOW(), INTERVAL 7 DAY)
                  AND category IN ('COMMUNITY', 'TRADE', 'QNA')
            ) t
            WHERE rn = 1
        )
        UNION ALL
        (
            SELECT
                id, title, content, category, created_at,
                like_count AS likeCount,
                like_count AS score,
                'HOT_TREND' AS reasonType
            FROM post
            WHERE created_at >= DATE_SUB(NOW(), INTERVAL 7 DAY)
        )
    ) AS combined_posts
    ORDER BY score DESC
    LIMIT 3
    ]]>
    </select>

    <!--    추가 2/13 -->
    <update id="updateContent">
        UPDATE post
        SET content = #{content}
        WHERE id = #{id}
    </update>

    <update id="updatePostStatsBatch" parameterType="java.util.List">
        <foreach collection="list" item="item" separator=";">
            UPDATE post
            SET
            like_count = like_count + #{item.likeCount},
            view_count = view_count + #{item.viewCount}
            WHERE id = #{item.id}
        </foreach>
    </update>

    <select id="getTopPostList" resultType="map">
        SELECT
        id, view_count, like_count,
        ((like_count * 10) + view_count) / POW(TIMESTAMPDIFF(HOUR, created_at, NOW()) + 2, 1.8) AS score
        FROM post
        WHERE post_status = 'ACTIVE'
        AND created_at >= DATE_SUB(NOW(), INTERVAL #{i} DAY)
        ORDER BY score DESC
        LIMIT 100
    </select>

    <select id="findByKeywordPosts" resultType="com.daedong.zipmap.domain.PostDTO" parameterType="string">
        SELECT id,title,content,category,created_at AS createdAt,like_count AS likeCount
        FROM post
        WHERE post_status = 'ACTIVE'
        AND title LIKE CONCAT('%', #{keyword}, '%')
        OR content LIKE CONCAT('%', #{keyword}, '%')
        AND post_status = 'ACTIVE'
        ORDER BY created_at DESC
        LIMIT 3
    </select>

</mapper>
